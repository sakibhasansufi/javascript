** sum of an array
function sumOfNumbers(numbers) {
    let sum = 0;
    for (number of numbers){
        sum = sum +number;
        console.log(number);
    }
    return sum;

}

const numbs = [98, 46, 98, 57, 25];
const total = sumOfNumbers(numbs);
console.log('sum of the number is', total);
